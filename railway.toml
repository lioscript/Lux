[build]
buildCommand = "npm install -g pnpm@10.26.1 && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "cd artifacts/api-server && node dist/index.mjs"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 300
